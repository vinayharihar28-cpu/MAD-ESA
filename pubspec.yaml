name: pipe_measure_app
description: A new Flutter project

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
    flutter:
      sdk: flutter

    camera: ^0.10.5

    cupertino_icons: ^1.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter

  camera: ^0.10.5
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets: assets/data/student.json
